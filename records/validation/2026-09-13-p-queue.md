# Approved external-repository validation: p-queue

Date: 2026-09-13
PostCode implementation: `3c66971` (unchanged during this exercise)
Authorization: [active task follow-up](../tasks/2026-09-12-initial-module-inventory.md), committed as `4b78a91`

The human approved p-queue for the unfamiliar-repository exercise and explicitly
left Unicode acceptance pending. This exercise verifies the configured CLI path;
it does not establish p-queue runtime correctness or human usability acceptance.

## Inputs and reproduction conditions

- Repository: <https://github.com/sindresorhus/p-queue>
- Exact revision: `180ab9e25cd10b6f548767d7176076b50d25e188`
- Retained checkout: `/private/tmp/postcode-p-queue-validation-20260913`
- Selected configuration: the unchanged root `tsconfig.json`, which extends
  `@sindresorhus/tsconfig` and includes `source`.
- Analysis and PostCode verification: Node 22.13.1, npm 11.17.0, PostCode's pinned
  TypeScript 6.0.3. The analyzed repository's installed TypeScript 5.9.3 does not
  replace PostCode's compiler API.
- Dependency installation ran in the external checkout with Node 25.9.0 and npm
  11.12.1. All lifecycle scripts were disabled. No p-queue build, test, benchmark,
  or target application code was executed.

Acquisition used `git clone --depth 1` from the approved URL. The checkout had no
committed lockfile. `npm install --ignore-scripts --no-audit --no-fund` installed
518 packages with a task-local npm cache. An offline, script-disabled
`npm install --package-lock-only --package-lock=true` captured the resolved tree.
The only added checkout file is that local lockfile; tracked inputs remain clean.
Its exact copy is retained with the validation artifacts, not committed here.

Resolved analysis-relevant packages include `@sindresorhus/tsconfig` 8.1.0,
`eventemitter3` 5.0.4, `p-timeout` 7.0.2, and `@types/node` 25.9.6. The inherited
configuration and selected dependency tree are part of the captured input state;
no replacement or reduced validation configuration was introduced.

Run from the PostCode checkout after `npm ci` and `npm run build`:

```sh
node _build/src/cli.js modules --project /private/tmp/postcode-p-queue-validation-20260913/tsconfig.json --json
node _build/src/cli.js modules --project /private/tmp/postcode-p-queue-validation-20260913/tsconfig.json
node _build/src/cli.js inspect <handle-from-inventory> --project /private/tmp/postcode-p-queue-validation-20260913/tsconfig.json --json
node _build/src/cli.js inspect <handle-from-inventory> --project /private/tmp/postcode-p-queue-validation-20260913/tsconfig.json --json --source-detail
```

Recorded snapshot IDs depend on the captured environment and absolute configured
input paths. A reproduction elsewhere must learn its own current handles; no
cross-snapshot continuity is implied.

## Results

Before installation, the missing inherited configuration produced exit 2,
diagnostic TS6053, and no view. After installation, all exercised CLI invocations
returned exit 0 and submitted local observation batches.

The inventory has seven modules: the five configured project source modules and
the two reached external declaration modules. A separate public TypeScript API
probe using its standard host confirmed the same seven external-module
SourceFiles, no ambient modules, and five configured roots. The full Program
contains 70 source files; non-module library sources are not silently counted as
modules.

Two independent JSON process outputs are byte-identical. Unicode and JSON agree
on `snapshot:179143ff7a93407189dccdd49d25150de34d4005beec8fc70c10c716408902b6`.
Discovery plus all seven export and seven documentation expansion scopes are
fully materialized within their declared guarantees. No whole-program semantic
or runtime correctness claim is made.

Each project module was selected by its inventory handle and inspected with
explicit source detail. The five complete export-name sets matched a direct
reading of the pinned source declarations. Checks covered type-only forwarding,
dual-role class exports, value-only function exports, re-export origins, and the
entry export's associated documentation retaining recorded-assertion status.
Normal inventory and inspection JSON omit source-evidence fields; explicit detail
identifies the relevant declarations, including external re-export origins.

The Unicode inventory is 7,174 bytes. It exposes precise handles and export
relationships, but anonymous module names and compiler `default` symbol names
still limit immediate recognition. This is an implementation-agent observation,
not the required human usability judgment. No defect requiring an implementation
change was found in this exercise.

All 37 PostCode tests and `npm run check` passed again. The complete branch diff
passes whitespace checks. The task remains active: human Unicode feedback and
final independent integrated review are outstanding.

## Retained evidence

Artifacts are retained locally under `_observations/validation/2026-09-13/p-queue/`,
excluded from analysis inputs and ignored by Git. They include both inventory
JSON invocations, Unicode, normal inspection, five explicit source inspections,
initial open-failure output, check results, toolchain/input metadata, the resolved
lockfile, and a SHA-256 manifest. Normal invocation batches are also retained in
the configured local `_observations/` sink. No third-party repository contents or
real-project observations are committed.

| Artifact | SHA-256 |
| --- | --- |
| `modules.json` | `8015e7d55691da0423da2a21973168ad83ab62719e2cb72503415cbdb0c05df9` |
| `modules-unicode.txt` | `bc92825dd6ca9ed303c0092aa7138773112cc965c4a37a931367d87f135f6d59` |
| `checks.json` | `d9e0e6922668c7d5b6cd2feb6f7d8fea338eb1befdb06708aa5975e0856bf287` |
| `environment.json` | `29692f21e170ac1553858b7f0d40584060b1c217e0829e8de41a7e893012996e` |
| `resolved-package-lock.json` | `89f528597c18113ebd16751ef0c3db4e0d95db0e324f3e75169d7ac17d7c879a` |
| `manifest.json` | `fd9f85d7fc7b51c9f7ce9b9f9b24dfa3dcadcd11b17c118ab3acb1e031db76cc` |
