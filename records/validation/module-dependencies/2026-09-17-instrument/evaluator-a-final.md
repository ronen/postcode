Final addendum based only on the five corrected captures. Prior evaluations remain historical.

The identified meanings now read clearly:

- Root components explicitly have no incoming project relationships from outside the component; the cycle grouping is explicitly not an entity.
- Organization vocabulary now defines `same-group`, `into-descendants`, and `outward`, while separating layout classifications from policy judgments.
- “Re-exports only” is separated from edge annotations, including explicit “Module property” lines in parents/children views.
- Projection counts, project population, discovered lookup population, and display omissions are distinguished. The boundary’s 13 project modules versus 14 discovered modules is intelligible alongside opaque external `t`.
- Supported-source-request coverage is qualified near the header, and display limits remain distinct from analysis limits. “Further descent pruned” improves the interpretation of the visible `m6 → m7` edge.

One remaining material navigation ambiguity: structure views label the source-detail command “captured request and organization source detail,” but that command omits the snapshot argument used in the other navigation commands. From these views alone, I cannot tell whether it preserves the displayed capture or obtains fresh results. The text should make that distinction clear.

Minor friction remains: inspect still uses `MODULE_HANDLE` while dependency views consistently use `ENTITY_ID`. Concrete group memberships are not shown in the summary, but the newly supplied source-detail route makes their absence understandable. I see no remaining material ambiguity in the requested graph or qualification meanings themselves.
