The corrected views resolve most earlier presentation concerns:

- **Cycle headers:** comma-separated membership no longer implies pairwise edges. The root-component definition also clarifies structural roots.
- **Organization:** explicit definitions now distinguish equal groups, strict organization descendants, and targets outside the source group and its descendants. Unknown organization remains clearly separate from policy failure.
- **Badges:** “re-exports only” now appears on module/group entries rather than relationship rows, removing the earlier syntax-versus-edge confusion.
- **Populations:** projection, materialized project, and discovered lookup counts are explicitly distinguished. The earlier apparent contradiction is resolved.
- **Omissions:** module, relationship, and descent limits appear before the graph. Detailed request/coverage omission counts remain below, but the overview no longer initially appears complete.
- **Individual rows:** numbered Requests and Coverage outcomes are distinguishable within the view, explicitly not entity selectors. Twenty displayed Coverage rows plus 24 omitted now reconcile with the total of 44.

Remaining material ambiguity: numbered rows still lack target text or locations, and “owner not shown/established” combines information hidden by presentation with information not established by analysis. Source-detail navigation offers a useful next step, but the structure views label that step “captured” while their command lacks a snapshot argument; these captures alone cannot establish whether it preserves the displayed analysis context. The focused children source-detail command does include the snapshot.
